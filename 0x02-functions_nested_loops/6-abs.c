#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @f: int type number
 * Return: absolute value of @f
 */
int _abs(int f)
{
	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		return (n);
	}
}
